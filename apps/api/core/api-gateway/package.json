{
  "name": "api-gateway",
  "scripts": {
    "build": "dotnet build",
    "build:compose": "docker compose down && docker compose build",
    "dev": "docker compose up",
    "down": "docker compose down"
  }
}
