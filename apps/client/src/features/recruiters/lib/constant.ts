export interface Recruiter {
  name: string;
  location: string;
  description: string;
  profilePictureUrl: string;
  id: string;
  isVerified: boolean;
  createdAt: string;
  isActive: boolean;
  userId: string;
  updatable: string;
}
